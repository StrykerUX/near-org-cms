import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/chain-abstraction",
  title: "Chain Abstraction",
  description:
    "Chain abstraction makes every chain feel like one system. Hold, swap, and move any asset across 35+ chains from a single account — no bridges, no wrapped tokens, no gas to juggle.",
  blurb: "Every chain, one account",
  nav: { header: true, footer: false, label: "Chain Abstraction", order: 22 },
  sitemap: { changeFrequency: "monthly", priority: 0.8 },
} satisfies PageMeta;

export default meta;
