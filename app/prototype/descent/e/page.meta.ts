import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/e",
  title: "Descenso · E escalera por turnos, hero pegado",
  description:
    "Igual que D en el reparto de la escalera, pero el hero queda pegado mientras la figura se dibuja, así su imagen cubre la zona hasta que está completa.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
