import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/g",
  title: "Descenso · G la escalera entra en turnos",
  description:
    "La figura nace completa, pero cada columna arranca mas abajo y sube a su sitio en turnos, de los extremos al centro. Movimiento sin que la silueta degenere en rectangulo.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
