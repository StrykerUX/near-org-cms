import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/f",
  title: "Descenso · F la escalera ya está ahí",
  description:
    "La figura de la escalera nace completa y no se anima: el scroll la descubre. Desde el primer pixel asoma el borde escalonado, sin pasar nunca por una banda gris plana.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
