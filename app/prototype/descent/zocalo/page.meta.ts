import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/zocalo",
  title: "Descenso · C zócalo",
  description:
    "Approach C: misma reja y misma geometría que producción, con los escalones arrancando todos en 0 y con la misma ease, para que la silueta sea una escalera proporcionada en todo instante.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
