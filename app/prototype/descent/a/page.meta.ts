import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/a",
  title: "Descenso · A retención",
  description:
    "Approach A: el hero se retiene con un transform de compensación y los escalones siguen la curva; el bloque gris queda exento porque es la tapa de la juntura.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
