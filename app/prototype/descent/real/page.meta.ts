import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/real",
  title: "Descenso · original",
  description:
    "El hero y la escalera de producción sin modificar, con el panel de medición encima. Es el 'antes' contra el que se comparan los approaches del laboratorio.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
