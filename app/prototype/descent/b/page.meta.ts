import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/b",
  title: "Descenso · B track compartido",
  description:
    "Approach B: hero y escalera dentro de un mismo track sticky, así la juntura no puede separarse. El ritmo es una curva sobre el progreso del track.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
