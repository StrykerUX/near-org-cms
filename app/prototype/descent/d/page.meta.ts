import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/d",
  title: "Descenso · D escalera por turnos, imagen estirada",
  description:
    "La escalera se dibuja de los bordes al centro columna por columna, sin banda gris plana, y el statement sube montado en el último turno. La imagen del hero se estira por debajo para que los huecos muestren montaña.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
