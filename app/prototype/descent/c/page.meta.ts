import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/c",
  title: "Descenso · C sin traslación",
  description:
    "Approach C: el hero no se traslada nunca; el ritmo lo da solo el movimiento de su contenido interno. Riesgo geométrico cero.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
