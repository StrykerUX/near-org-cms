import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/descent/lab",
  title: "Descenso · baseline",
  description:
    "La maqueta del laboratorio sin animación de descenso: la geometría del hero y la escalera con el scrub lineal de hoy. Es la referencia contra la que se comparan los approaches.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
