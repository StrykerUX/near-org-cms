import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/flow-compare",
  title: "Flow field · comparación",
  description:
    "A/B entre el campo de flujo reimplementado en este repo y el mismo efecto servido por el SDK de Unicorn Studio.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
