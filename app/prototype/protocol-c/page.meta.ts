import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-c",
  title: "Protocol C — Field",
  description:
    "La página Protocol entera, abierta por Field. Un campo de caracteres monoespaciados con las palabras del protocolo sembradas entre ruido, encendidas por una onda diagonal lenta. Mismas siete secciones que /prototype/protocol-a: lo único distinto es el hero.",
  blurb: "Protocol C · Field",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
