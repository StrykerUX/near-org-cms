import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/solutions-b",
  title: "Solutions — Propuesta B · El conmutador",
  description:
    "Propuesta para /solutions. Tesis: un hub es una interfaz, no un artículo — las cinco soluciones viven en una sola pantalla con selector navegable por teclado, y elegir cuesta un clic y cero scroll. El dibujo es un panel de conexión que cambia de parcheo, no de tema. No reemplaza a la página real.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
