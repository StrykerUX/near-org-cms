import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/homepage-g",
  title: "Homepage G",
  description:
    "El hero se pliega: el paisaje del shader se comprime hasta caber en un objeto que ocupa el lugar de la palabra «your» — «Own your world.» se vuelve «Own ⬡ world.». En esta variante el objeto es una caja de esquinas blandas con el follaje dentro y el mark de NEAR encima. De la cintura para abajo es la misma página que /prototype/homepage-e.",
  blurb: "el hero plegado, el logo dentro de la caja",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
