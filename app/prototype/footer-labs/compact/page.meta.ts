import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/compact",
  title: "Footer 02 · Compact",
  description:
    "The production footer without its headline and with Resources and About opened into two columns, so the height it gives back goes to the wordmark.",
  blurb: "Shorter panel, bigger logo",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
