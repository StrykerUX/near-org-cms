import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/veil",
  title: "Footer 01 · Veil",
  description:
    "The production footer with a gradient the colour of the background falling over the wordmark, and the logo flush to the bottom edge so it reads as sliding underneath the page.",
  blurb: "The logo sinks under the footer",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
