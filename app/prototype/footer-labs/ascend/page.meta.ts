import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/ascend",
  title: "Footer 03 · Ascend",
  description:
    "The production takeover without the scroll tug: it starts when the reader reaches the bottom on their own, the content cascades in, and the panel is sized in svh so it never gets cut off in short viewports.",
  blurb: "The original wipe, without the tug",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
