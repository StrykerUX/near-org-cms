import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/ascend",
  title: "Footer 03 · Ascend",
  description: "El takeover de producción sin el tirón de scroll: arranca cuando el lector llega solo al fondo, el contenido entra en cascada y el panel se comprime en svh para no cortarse en viewports bajos.",
  blurb: "El wipe original, sin tirón",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
