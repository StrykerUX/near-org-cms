import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/kinetic",
  title: "Footer 05 · Kinetic",
  description: "Footer editorial en flujo que no tapa nada: entra por líneas enmascaradas, columnas escalonadas y un barrido lateral sobre el wordmark. Se ve una vez.",
  blurb: "Editorial, entrada única",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
