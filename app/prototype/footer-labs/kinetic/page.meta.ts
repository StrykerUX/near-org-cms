import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/kinetic",
  title: "Footer 05 · Kinetic",
  description:
    "An editorial footer in flow that covers nothing: it enters through masked lines, staggered columns and a lateral sweep across the wordmark. Seen once.",
  blurb: "Editorial, one entrance only",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
