import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/reveal",
  title: "Footer 04 · Reveal",
  description: "El footer está fijo al fondo del viewport desde el primer frame y la página es una hoja opaca que se desliza hacia arriba y lo descubre. Sin coreografía.",
  blurb: "Se descubre por debajo",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
