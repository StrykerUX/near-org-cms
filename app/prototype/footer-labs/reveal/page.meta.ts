import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/footer-labs/reveal",
  title: "Footer 04 · Reveal",
  description:
    "The footer is fixed to the bottom of the viewport from the first frame and the page is an opaque sheet that slides up and uncovers it. No choreography.",
  blurb: "Uncovered from underneath",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
