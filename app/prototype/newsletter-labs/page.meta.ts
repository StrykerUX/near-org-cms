import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/newsletter-labs",
  title: "Newsletter lab · 8 variantes",
  description:
    "Ocho versiones de la banda «NEAR belongs to you» con la misma copy: cartel, línea de escritura, banda partida, campo dentro de la frase, halo de luz, grano sobre lima, columna con retícula y verde de marca a sangre. Sin las escaleras de la sección actual. Ver components/sections/newsletter-labs/README.md.",
  blurb: "8 versiones de la banda de newsletter",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
