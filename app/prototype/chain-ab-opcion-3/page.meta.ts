import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/chain-ab-opcion-3",
  title: "Chain Abstraction — Opción 3",
  description:
    "Igual que la propuesta C, con una versión propia de «Built for what transacts next»: una marca de cuenta que no se mueve y un operador que se releva debajo. Fondo crema, sin gradiente. No reemplaza a la página real.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
