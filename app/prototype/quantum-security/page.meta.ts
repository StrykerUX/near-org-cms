import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/quantum-security",
  title: "Quantum Security",
  description:
    "Port of the quantum-security rebuild onto the repo's design system — own sections, hardcoded copy, no real data.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
