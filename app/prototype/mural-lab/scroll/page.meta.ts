import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/mural-lab/scroll",
  title: "Mural Lab · Scroll",
  description: "Seis variantes de la Section #2 donde el avance de la animacion es la posicion del scroll, con el ritmo puesto por el gesto del lector en vez de por el diseno.",
  blurb: "6 atadas al scroll",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
