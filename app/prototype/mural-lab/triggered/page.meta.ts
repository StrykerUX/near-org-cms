import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/mural-lab/triggered",
  title: "Mural Lab · Trigger",
  description: "Ocho variantes de la Section #2 con timeline propia: el scroll decide cuando empiezan y cuando se deshacen, pero las duraciones y las curvas son siempre las mismas.",
  blurb: "8 con timeline propia",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
