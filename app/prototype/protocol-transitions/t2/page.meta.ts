import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-transitions/t2",
  title: "Transición · T2 — Aperture",
  description:
    "Cifras a escala de cartel recortadas por una banda más baja que ellas, desplazándose con el scroll. Ocupa 30svh, y se ve entre el hero real de la página y la sección que le sigue.",
  blurb: "transición T2 · Aperture (30svh)",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
