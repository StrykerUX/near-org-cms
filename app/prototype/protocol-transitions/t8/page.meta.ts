import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-transitions/t8",
  title: "Transición · T8 — Grid",
  description:
    "Las doce columnas que gobiernan la página se dibujan una única vez y se desvanecen hacia abajo. Ocupa 50svh, y se ve entre el hero real de la página y la sección que le sigue.",
  blurb: "transición T8 · Grid (50svh)",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
