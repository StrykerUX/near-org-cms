import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-transitions/t4",
  title: "Transición · T4 — Handoff",
  description:
    "El puente hecho con lenguaje: una línea que convierte la afirmación del hero en la pregunta que el contenido responde. Ocupa 25svh, y se ve entre el hero real de la página y la sección que le sigue.",
  blurb: "transición T4 · Handoff (25svh)",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
