import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-transitions/t12",
  title: "Transición · T12 — Genesis",
  description:
    "La plancha isométrica que protagoniza el acto central nace acá, shard por shard. Ocupa 90svh, y se ve entre el hero real de la página y la sección que le sigue.",
  blurb: "transición T12 · Genesis (90svh)",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
