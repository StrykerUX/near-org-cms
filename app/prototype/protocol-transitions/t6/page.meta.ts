import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-transitions/t6",
  title: "Transición · T6 — Split",
  description:
    "Las cifras partidas por la línea entre el crema y el blanco; al scrollear se alinean y cosen el corte. Ocupa 50svh, y se ve entre el hero real de la página y la sección que le sigue.",
  blurb: "transición T6 · Split (50svh)",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
