import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-heroes/h4",
  title: "Protocol hero · H4 — Cut",
  description:
    "Prueba fuera: el hero mide 78svh y la banda de cifras asoma cortada por el borde inferior del viewport.",
  blurb: "hero H4 · Cut",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
