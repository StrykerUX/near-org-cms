import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-heroes/h7",
  title: "Protocol hero · H7 — Mural",
  description:
    "Prueba fuera: se agranda la categoría y no la afirmación — «agent economy» cruzando la página de borde a borde.",
  blurb: "hero H7 · Mural",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
