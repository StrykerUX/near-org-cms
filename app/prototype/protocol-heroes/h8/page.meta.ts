import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-heroes/h8",
  title: "Protocol hero · H8 — Terminal",
  description:
    "Prueba dentro, como status line, en el único hero oscuro de las ocho, con el sheen recorriendo el titular.",
  blurb: "hero H8 · Terminal",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
