import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-heroes/h3",
  title: "Protocol hero · H3 — Threshold",
  description:
    "Prueba fuera: el hero solo declara, y la banda de cifras entra pegada bajo el nav durante el primer tramo de la página.",
  blurb: "hero H3 · Threshold",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
