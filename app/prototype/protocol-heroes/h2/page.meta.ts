import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-heroes/h2",
  title: "Protocol hero · H2 — Count",
  description:
    "Prueba dentro del hero, como marcador a sangre en el borde inferior, con las seis cifras contando al entrar.",
  blurb: "hero H2 · Count",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
