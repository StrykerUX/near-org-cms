import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-heroes/h5",
  title: "Protocol hero · H5 — Index",
  description:
    "Prueba fuera: en su lugar, el índice de la página — las seis capacidades del protocolo, numeradas y enlazadas.",
  blurb: "hero H5 · Index",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
