import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/homepage-i",
  title: "Homepage I",
  description:
    "Igual que /prototype/homepage-g salvo por cómo entra el NEAR Stack: el fondo se da vuelta en un solo frame, seco, sin transición — como pasar la página de una revista. El título ya está puesto y pasa de tinta a crema con el mismo corte.",
  blurb: "la obertura del stack, inversión seca",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
