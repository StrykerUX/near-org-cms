import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-proof/p5",
  title: "Proof divider · P5 — Sentence",
  description:
    "Las seis cifras dentro de una oración, a cuerpo de lectura: se lee como el pie del hero. Se ve entre el hero real de la página y la sección que le sigue, que es la única forma de juzgar una juntura.",
  blurb: "divider P5 · Sentence",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
