import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-proof/p6",
  title: "Proof divider · P6 — Live",
  description:
    "Barra de estado oscura, el corte más marcado de las ocho. Requiere conectar el RPC. Se ve entre el hero real de la página y la sección que le sigue, que es la única forma de juzgar una juntura.",
  blurb: "divider P6 · Live",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
