import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-proof/p1",
  title: "Proof divider · P1 — Hierarchy",
  description:
    "El uptime manda y las otras cinco lo acompañan en una línea: seis datos del mismo tamaño son cero datos. Se ve entre el hero real de la página y la sección que le sigue, que es la única forma de juzgar una juntura.",
  blurb: "divider P1 · Hierarchy",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
