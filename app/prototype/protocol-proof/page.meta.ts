import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-proof",
  title: "Protocol — ocho franjas de prueba",
  description:
    "Laboratorio de la franja de seis cifras de la página Protocol: ocho tratamientos que cambian la forma y, en la mitad de los casos, también qué se dice del dato.",
  blurb: "ocho maneras de probarlo",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
