import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-proof/p2",
  title: "Proof divider · P2 — Benchmark",
  description:
    "Cada cifra con una línea que la traduce para quien no trabaja en infraestructura. Se ve entre el hero real de la página y la sección que le sigue, que es la única forma de juzgar una juntura.",
  blurb: "divider P2 · Benchmark",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
