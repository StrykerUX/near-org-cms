import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-proof/p3",
  title: "Proof divider · P3 — Grouped",
  description:
    "Tres pares con rótulo —Speed, Scale, Record— separados por filetes: tres ideas en vez de seis números. Se ve entre el hero real de la página y la sección que le sigue, que es la única forma de juzgar una juntura.",
  blurb: "divider P3 · Grouped",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
