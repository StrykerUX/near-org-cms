import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-proof/p7",
  title: "Proof divider · P7 — Axis",
  description:
    "Un eje que cruza la página con las seis cifras alternadas: la forma que la homepage ya usa. Se ve entre el hero real de la página y la sección que le sigue, que es la única forma de juzgar una juntura.",
  blurb: "divider P7 · Axis",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
