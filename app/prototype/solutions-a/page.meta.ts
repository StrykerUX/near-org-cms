import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/solutions-a",
  title: "Solutions — Propuesta A · Índice",
  description:
    "Propuesta para /solutions. Tesis: el lector llega con su caso de uso decidido, así que el índice de las cinco soluciones es la primera pantalla, y después se queda pegado mientras los cuerpos desfilan. Sin superficie WebGL: la textura es la retícula. No reemplaza a la página real.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
