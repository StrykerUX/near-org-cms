import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/about-b",
  title: "About B · Instrumento",
  description:
    "La historia de NEAR como aparato: página oscura, paneles con lectura y riel de actos. Mismo armazón que las variantes B del resto; el vocabulario gráfico es propio.",
  blurb: "estratos: ocho años como capas que se acumulan",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
