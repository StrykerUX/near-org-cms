import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/quantum-security-copy",
  title: "Quantum Security (copy)",
  description: "Duplicate of /quantum-security for iterating without touching the live page.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
