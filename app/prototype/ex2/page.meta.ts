import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/ex2",
  title: "EX2 · field",
  description:
    "Draft of the typographic hero over a WebGL contour field that drifts very slowly, with the headline centred and two actions at the foot.",
  blurb: "Typographic hero over a WebGL field",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
