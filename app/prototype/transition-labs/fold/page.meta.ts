import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/transition-labs/fold",
  title: "Transition · G · Fold",
  description:
    "Transition G: the page folds backwards on its bottom edge and the next section appears behind it.",
  blurb: "The page folds away",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
