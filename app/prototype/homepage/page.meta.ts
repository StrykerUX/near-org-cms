import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/homepage",
  title: "Homepage draft",
  description: "Draft de la landing nueva de NEAR — secciones individuales, sin datos reales.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
