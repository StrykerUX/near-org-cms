import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/homepage-ab10",
  title: "Homepage AB10",
  description:
    "Exact clone of Homepage AB9 — starting point for the layout-and-sticky-changes branch. ab9 stays as the rollback. Its own sections in components/sections/home-ab10/, hardcoded copy, no real data.",
  blurb: "ab9 clone, layout and sticky work in progress",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
