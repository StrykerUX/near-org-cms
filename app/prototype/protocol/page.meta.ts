import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol",
  title: "Protocol",
  description:
    "NEAR Protocol page from the sitemap doc — own sections, hardcoded copy, no real data.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
