import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/homepage-h",
  title: "Homepage H",
  description:
    "Igual que /prototype/homepage-g salvo por cómo entra el NEAR Stack: el objeto que quedó en «Own ⬡ world.» baja cruzando la pantalla y deja el negro detrás, como un pincel. El título llega antes que la sección, sobre crema, y se invierte por donde el negro ya pasó.",
  blurb: "la obertura del stack, con el objeto de pincel",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
