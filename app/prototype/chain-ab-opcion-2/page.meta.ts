import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/chain-ab-opcion-2",
  title: "Chain Abstraction — Opción 2",
  description:
    "Igual que la propuesta C, con una versión propia de «Built for what transacts next»: una sola hairline full-bleed con tres paradas, que entra y sale de cuadro sin cortarse. Fondo crema, sin gradiente. No reemplaza a la página real.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
