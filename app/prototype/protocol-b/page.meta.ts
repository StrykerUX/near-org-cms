import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-b",
  title: "Protocol B — Spectrum",
  description:
    "La página Protocol entera, abierta por Spectrum. Bandas verticales en interferencia, en un shader WebGL. La columna atraviesa el hero: la superficie no se retira, se convierte en el layout. Mismas siete secciones que /prototype/protocol-a: lo único distinto es el hero.",
  blurb: "Protocol B · Spectrum",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
