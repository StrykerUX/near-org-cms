import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-b",
  title: "Protocol · B — The Machine",
  description:
    "Alternativa B para la página de Protocol: la mecánica primero. Una sola pieza isométrica atraviesa la página cambiando de estado, con las seis capacidades del protocolo como beats de un acto pegado y las seis cifras como telemetría del objeto.",
  blurb: "protocol como una máquina que muta",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
