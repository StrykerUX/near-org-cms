import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/homepage-ab7",
  title: "Homepage AB7",
  description:
    "Fork of Homepage AB6 with the v5 hero (art-glass clip) and its NearStackV2 — ab6 stays as the rollback. Its own sections in components/sections/home-ab7/, hardcoded copy, no real data.",
  blurb: "ab6 with the v5 hero and stack",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
