import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/hero-alt",
  title: "Hero Lab · 6 versiones",
  description:
    "Seis versiones alternativas de las dos primeras secciones de la homepage (hero + statement), con la misma copy: CSS/GSAP, campo de flujo WebGL, transforms 3D, vidrio refractante, retícula en canvas 2D y el clip de v5 recortado a los glifos. Ver components/sections/hero-alt/README.md.",
  blurb: "6 heroes alternativos, misma copy",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
