import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/hero-alt",
  title: "Hero Lab · 6 versions",
  description:
    "Six alternative versions of the homepage's first two sections (hero + statement), all with the same copy: CSS/GSAP, a WebGL flow field, 3D transforms, refracting glass, a canvas 2D lattice, and the v5 clip masked to the glyphs. See components/sections/hero-alt/README.md.",
  blurb: "6 alternative heroes, same copy",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
