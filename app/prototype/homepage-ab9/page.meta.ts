import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/homepage-ab9",
  title: "Homepage AB9",
  description:
    "Fork of Homepage AB7 without QuantumBars — the section, its stair animation and its statement are gone, and ab7 stays as the rollback. Its own sections in components/sections/home-ab9/, hardcoded copy, no real data.",
  blurb: "ab7 without the quantum bars",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
