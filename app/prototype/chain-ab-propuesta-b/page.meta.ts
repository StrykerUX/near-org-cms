import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/chain-ab-propuesta-b",
  title: "Chain Abstraction — Propuesta B",
  description:
    "Propuesta para /chain-abstraction sobre el sistema visual del homepage real (/prototype/homepage-update). Se diferencia de la propuesta A en el Proof: titular acentuado, contadores en lista y gráfica al lado, en dos columnas. Cierra con las tres secciones finales tal cual están en la página real. No reemplaza a la página real.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
