import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/chain-ab-propuesta-c",
  title: "Chain Abstraction — Propuesta C",
  description:
    "Variante de la propuesta B: mismas cuatro secciones propias (Hero, Why it matters, Capabilities sticky-scroll y Proof, este último con el strip de logos en carrusel infinito), y a continuación las tres secciones finales tal cual están en la página real de /chain-abstraction. No reemplaza a la página real.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
