import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/chain-ab-opcion-1",
  title: "Chain Abstraction — Opción 1",
  description:
    "Igual que la propuesta C, con una versión propia de «Built for what transacts next»: díptico de dos actores —una persona hoy, un agente después— que bajan a un único nodo de cuenta. Fondo crema, sin gradiente. No reemplaza a la página real.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
