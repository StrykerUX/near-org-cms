import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/stack-labs",
  title: "Stack lab · 8 layouts",
  description:
    "Eight layouts for the NEAR Stack section, all on the same isometric assembly: bleeding off, editorial without sticky, with the text anchored to each layer, as an engineering drawing, and with the camera travelling across the layers. See components/sections/stack-labs/README.md.",
  blurb: "8 layouts for the NEAR Stack",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
