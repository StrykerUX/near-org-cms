import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/stack-labs",
  title: "Stack lab · 5 layouts",
  description:
    "Cinco layouts para la sección del NEAR Stack, sobre el mismo ensamble isométrico: a sangre, editorial sin sticky, con el texto anclado a cada capa, como plano de ingeniería, y con la cámara recorriendo las capas. Ver components/sections/stack-labs/README.md.",
  blurb: "5 layouts para el NEAR Stack",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
