import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/stack-labs/traveling",
  title: "Stack lab · E · Traveling",
  description:
    "Variant E of the NEAR Stack: split screen with the art in close-up on the active layer and that layer at headline size. 380svh track.",
  blurb: "Stack with the camera travelling the assembly",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
