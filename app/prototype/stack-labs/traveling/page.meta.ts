import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/stack-labs/traveling",
  title: "Stack lab · E · Traveling",
  description:
    "Variante E del NEAR Stack: pantalla partida con el arte en plano cerrado sobre la capa activa y esa capa a tamaño de titular. Track de 380svh.",
  blurb: "Stack con la cámara recorriendo el ensamble",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
