import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-combo/g",
  title: "Combo · G · Field claro + Board",
  description:
    "El hero claro seguido de un tablero de celdas asimétricas donde el título, las seis cifras y las tres propiedades se leen a la vez, separados sólo por filetes. La página va entera hasta el cierre: lo que hay que ver es qué le hacen estas dos secciones al acto.",
  blurb: "combo g · Board",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
