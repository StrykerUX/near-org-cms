import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-combo/layerflow",
  title: "Combo · C capas — Strata",
  description:
    "Seis estratos horizontales apilados con paralaje, sobre crema. La palabra que el titular usa para describir a NEAR es «layer», dos veces; el fondo la dibuja. Misma composición que c-light, cambia sólo la superficie.",
  blurb: "combo layerflow · Strata",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
