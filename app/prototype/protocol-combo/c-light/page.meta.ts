import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-combo/c-light",
  title: "Combo · C claro — Haze",
  description:
    "Luz difusa sobre crema: un degradé abollado por un campo de ruido lento, sin una sola estructura que compita con el texto. Mismo método que el hero de la homepage. Misma composición que c-layers, cambia sólo la superficie.",
  blurb: "combo c-light · Haze",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
