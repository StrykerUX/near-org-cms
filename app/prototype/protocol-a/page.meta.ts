import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-a",
  title: "Protocol · A — Datasheet",
  description:
    "Alternativa A para la página de Protocol: la evidencia primero. El hero y la franja de seis cifras se funden en una sola pantalla, y las seis capacidades del protocolo van como una tabla de especificación con las seis filas abiertas a la vez.",
  blurb: "protocol como ficha técnica",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
