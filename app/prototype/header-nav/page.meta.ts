import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/header-nav",
  title: "Header & Nav",
  description: "Test bed for a new header / navigation menu design.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
