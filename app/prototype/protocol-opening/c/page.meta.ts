import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening/c",
  title: "Apertura · C — Spectrum",
  description:
    "Bandas verticales en interferencia (shader). La columna atraviesa las tres secciones: la superficie no se retira, se convierte en el layout. Se monta con el acto oscuro debajo, para ver si la apertura le come el rango.",
  blurb: "apertura C · Spectrum",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
