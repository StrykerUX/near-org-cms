import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening/f",
  title: "Apertura · F — Horizon",
  description:
    "Degradé profundo con banda de luz y grano (shader). La única cálida; el trío progresa de noche a día. Se monta con el acto oscuro debajo, para ver si la apertura le come el rango.",
  blurb: "apertura F · Horizon",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
