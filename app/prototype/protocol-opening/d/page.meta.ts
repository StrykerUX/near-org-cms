import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening/d",
  title: "Apertura · D — Stack",
  description:
    "Cuatro planos isométricos con paralaje, sólo SVG. El material de la marca a escala de superficie, sin WebGL. Se monta con el acto oscuro debajo, para ver si la apertura le come el rango.",
  blurb: "apertura D · Stack",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
