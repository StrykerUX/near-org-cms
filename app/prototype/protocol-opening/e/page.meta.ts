import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening/e",
  title: "Apertura · E — Field",
  description:
    "Retícula de caracteres en canvas con las palabras del protocolo escondidas entre ruido, encendidas por una onda diagonal. Se monta con el acto oscuro debajo, para ver si la apertura le come el rango.",
  blurb: "apertura E · Field",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
