import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening",
  title: "Protocol — tres aperturas",
  description:
    "Laboratorio de la apertura de la página Protocol: tres tríos completos (hero + las seis cifras + «Built for AI scale»), cada uno con su propia superficie — un shader WebGL y dos campos de caracteres en canvas.",
  blurb: "tres maneras de abrir la página",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
