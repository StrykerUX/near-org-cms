import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening",
  title: "Protocol — seis aperturas",
  description:
    "Laboratorio de la apertura de la página Protocol: seis tríos completos (hero + las seis cifras + «Built for AI scale»), cada uno con su propia superficie — cuatro shaders WebGL, un SVG con paralaje y un campo de caracteres en canvas.",
  blurb: "seis maneras de abrir la página",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
