import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening/a",
  title: "Apertura · A — Lattice",
  description:
    "Retícula isométrica en perspectiva (shader). Las tres secciones son un descenso: el hero flota sobre la red, las cifras se apoyan casi al ras y la superficie se agota antes del texto. Se monta con el acto oscuro debajo, para ver si la apertura le come el rango.",
  blurb: "apertura A · Lattice",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
