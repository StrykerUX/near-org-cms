import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/protocol-opening/b",
  title: "Apertura · B — Shards",
  description:
    "Campo de Voronoi que deriva (shader). La única superficie que explica en vez de acompañar; las cifras van dentro de regiones dibujadas. Se monta con el acto oscuro debajo, para ver si la apertura le come el rango.",
  blurb: "apertura B · Shards",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
