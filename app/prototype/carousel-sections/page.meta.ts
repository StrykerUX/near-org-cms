import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/carousel-sections",
  title: "Carousel Sections",
  description: "Test bed for new carousel-based home sections.",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
