import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/analytics/mix",
  title: "Analytics — Mix",
  description:
    "The assembly page for /analytics: one composition built out of sections taken from proposals A, B and C. Which section fills each slot is decided in AnalyticsMixView. Does not replace any real page.",
  blurb: "Sections from A, B and C, assembled",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
