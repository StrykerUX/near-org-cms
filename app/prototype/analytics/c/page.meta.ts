import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/analytics/c",
  title: "Analytics — Proposal C · Editorial",
  description:
    "A proposal for /analytics in an editorial register: large scale, nothing boxed, three full-width figures, and a hero background drawn from the real revenue series. The only one of the three that reveals on entry. Does not replace any real page.",
  blurb: "Analytics in an editorial register",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
