import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/analytics",
  title: "Analytics — three proposals",
  description:
    "Index of the /analytics lab: three structures for the same page, sharing the same copy and the same fifteen outbound links. They differ in how much space the page spends per datum. Does not replace any real page.",
  blurb: "Three structures for /analytics",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
