import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/analytics/b",
  title: "Analytics — Proposal B · Signal",
  description:
    "A proposal for /analytics in an instrument register: hero and figures on a single screen, an ambient strip for price and shards, a pair of cards with opposite tonal value, and tools grouped by task. Does not replace any real page.",
  blurb: "Analytics in an instrument register",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
