import type { PageMeta } from "@/lib/page-meta";

const meta = {
  route: "/prototype/analytics/a",
  title: "Analytics — Proposal A · Ledger",
  description:
    "A proposal for /analytics in a document register: high density, 1px rules, all five figures in a table, and the fifteen exits resolved in two identical tables. No animation. Does not replace any real page.",
  blurb: "Analytics in a document register",
  nav: false,
  sitemap: false,
  robots: "noindex",
} satisfies PageMeta;

export default meta;
